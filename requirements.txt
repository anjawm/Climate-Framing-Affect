# Python dependencies for the SDG 13 climate-discourse BERT/BERTopic analysis.
# Tested in Google Colab with Python 3.10.
#
# Versions are pinned to the tested combination. If you need to upgrade,
# please be aware that BERTopic and transformers occasionally introduce
# breaking changes.

# Core
pandas>=2.0,<3.0
numpy>=1.24,<2.0
openpyxl>=3.1                  # for reading the .xlsx training files
scikit-learn>=1.3,<2.0
matplotlib>=3.7

# Topic modeling
bertopic==0.16.4
sentence-transformers>=2.7
umap-learn==0.5.6
hdbscan==0.8.40
plotly>=5.18                   # for the BERTopic interactive HTML visualizations

# Coherence scoring (used inside the BERTopic notebook)
gensim>=4.3

# Deep learning / classification
torch>=2.1                     # use the appropriate +cu* wheel for your GPU
transformers>=4.41,<5.0

# Optional but useful for development
jupyterlab
